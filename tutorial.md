# Tutorial de Git y Linux

## Instalación de Git
- `sudo apt update`: Actualiza la lista de paquetes disponibles.
- `sudo apt install git -y`: Instala Git en el sistema.
- `git --version`: Verifica la versión instalada.
